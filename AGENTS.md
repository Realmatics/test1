## Cursor Cloud specific instructions

This is currently an empty/skeleton repository containing only a placeholder `README.md`. There are no application services, dependencies, build systems, or test frameworks configured.

- **No package manager or dependencies**: No `package.json`, `requirements.txt`, `pyproject.toml`, or similar dependency files exist.
- **No services to run**: No backend, frontend, database, or other services are defined.
- **No tests or linting**: No test framework or linter is configured.
- **No build system**: No build tooling is present.

When application code is added in the future, this section should be updated with:
- How to install dependencies
- How to run, build, and test the application
- Any non-obvious caveats for the development environment
